# Production Environment Configuration
# Copy this file to .env and update the values

# Database Configuration
DB_USER=ace_user
DB_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD
DB_NAME=ace_checkin

# API Key for mobile app authentication
# Generate with: openssl rand -base64 32
API_KEY=CHANGE_ME_GENERATE_SECURE_KEY

# Environment
ENVIRONMENT=production

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file: cp env.production.example .env
# 2. Generate secure values (use hex to avoid special chars):
#    - DB_PASSWORD: openssl rand -hex 32
#    - API_KEY: openssl rand -hex 32
# 3. Update the .env file with generated values
# 4. Keep .env file secure and never commit to git
